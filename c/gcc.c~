#include <stdio.h>

int main(){
  printf("Ultilizando o compilador gcc \n\n");
  printf("sintaxe basica:\n");
  printf("gcc [OPÇÕES] nome_do_arquivo\n");
  printf("algumas opcoes do compilador:\n");
  printf("-c: Compila o código fonte mas não faz as ligações. A saída é um arquivo objeto.\n");
  printf("-o: serve para dar um nome ao arquivo de saída.\n");
  printf("-g: salva os símbolos de depuração (o que permite usar um depurador)\n");
  printf("-Wall: ativa todos os avisos do compilador\n");  
  printf("-pedantic: ativa os avisos necessários para que o código esteja estritamente de acordo com os padrões\n\n");    
  printf("Para gerar o executável diretamente a partir do arquivo-fonte:\n");
  printf("gcc [OPÇÕES] -o programa[.bin] programa.c\n");
  printf("./programa\n\n");
  return(0);
}